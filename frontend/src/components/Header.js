import React from 'react';

function Header() {
    return(
        <header id="home" className="header">
            <h1 className="header-title">Portafolio de Cristian</h1>
            <p className="header-subtitle">Desarrollador Full Stack (React + Django)</p>
        </header>
    );
}

export default Header;